class dustbin
{
	constructor()
	{
        var foodStock;
		
		this.image=loadImage("sprites/milk.png")
    }
    display(){
    
    var x=80, y=100;
    imageMode(CENTER);
    this.image(this.image,720,220,70,70);

    if(this.foodStock=0){
        for (vari=0)
    }

}
    
}